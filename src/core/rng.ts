export class SevenBagRng { private bag: import('./types').PieceKind[] = []; constructor(private seed?: number){} private shuffle<T>(a:T[]):T[]{ for (let i=a.length-1;i>0;i--){ const j=Math.floor(Math.random()*(i+1)); [a[i],a[j]]=[a[j],a[i]]; } return a; } next(): import('./types').PieceKind { if (this.bag.length===0){ this.bag=['I','J','L','O','S','T','Z']; this.shuffle(this.bag); } return this.bag.pop()!; } }